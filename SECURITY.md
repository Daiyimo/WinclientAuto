# Security Policy

## 威胁模型

winclient-auto 是一个面向 AI Agent 的 Windows 客户端自动化框架。与传统人操作的 CLI 不同，
**Agent 可能基于不可信输入（用户 prompt、上传文件、其他 Agent 输出）自主构造并执行命令**。
这使得输入验证尤为关键。

### 攻击面

| 面 | 风险 | 缓解措施 |
|----|------|----------|
| 进程管理 | `wca app kill` 可终止任意进程（通过 `--process` 匹配） | 进程名匹配限制在已知应用范围内 |
| JavaScript 执行 | `wca dom eval --js <script>` 执行任意 JS | 仅在已连接的页面上下文中执行，不暴露给外部 |
| CDP 连接 | CDP 端口暴露调试能力 | 默认 localhost:9222，不监听外部接口 |
| 文件路径 | 截图输出路径可能被利用写入任意位置 | 建议使用相对路径，输出目录限定在项目内 |
| subprocess 调用 | Electron 应用通过 `--exe` 参数启动 | 不使用 `shell=True`，参数以列表传递 |

### Agent 使用安全准则

1. **始终使用 `--json` 输出** — 避免解析人类可读文本时的歧义
2. **验证连接状态** — 执行 DOM 操作前先 `wca app status --json` 确认连接有效
3. **限制 eval 使用** — `wca dom eval` 应尽量避免执行不可信的 JS 代码
4. **进程名精确匹配** — `--process` 参数使用足够精确的进程名片段，避免误杀

## 漏洞报告

如果你发现安全漏洞，请负责任地报告：

1. **不要开公开 GitHub issue**
2. 通过邮件联系维护者，或使用 GitHub 的私密漏洞报告：
   **Security** 标签页 > **Report a vulnerability**
3. 包含：受影响文件、复现步骤、潜在影响
4. 我们目标在 48 小时内确认报告，严重问题 7 天内修复

## 开发者安全准则

开发新的应用适配器或扩展框架时，遵循以下规则：

1. **不要在 `subprocess.run()` 中使用 `shell=True`** — 始终以列表形式传递参数
2. **验证所有外部输入** — 不要将用户控制的字符串直接传递给外部工具
3. **文件路径规范化** — 使用 `os.path.abspath()` 处理所有文件路径
4. **不在日志或 JSON 输出中暴露敏感信息** — 如 API 密钥、凭证等
