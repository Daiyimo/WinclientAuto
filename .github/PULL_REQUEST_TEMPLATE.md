## Description

<!-- 简要描述本 PR 的修改内容。 -->

Fixes #<!-- issue 编号 -->

## 修改类型

<!-- 勾选适用的类型： -->

- [ ] **新应用适配** — 在 `apps/` 下添加新的 Windows 应用适配层
- [ ] **新功能** — 为框架或现有适配器添加新功能
- [ ] **Bug 修复** — 修复错误行为
- [ ] **文档** — 仅更新文档
- [ ] **其他** — 请说明：

---

### 新应用适配检查清单

<!-- 如果本 PR 添加了新的应用适配，以下所有项必须勾选。 -->

- [ ] `apps/<app_name>/app.py` 存在且继承正确的基类
- [ ] `apps/<app_name>/constants.py` 包含应用专属配置
- [ ] Page Object 继承 `BasePage` 并实现必要操作
- [ ] 测试文件存在于 `tests/<app_name>/`
- [ ] README.md 已更新（如有必要）

### 现有代码修改检查清单

<!-- 如果本 PR 修改了现有代码，以下所有项必须勾选。 -->

- [ ] 所有单元测试通过：`pytest tests/framework/ -v`
- [ ] 无测试回归 — 没有移除或削弱之前通过的测试

### 通用检查清单

- [ ] 代码遵循现有风格和模式
- [ ] 新命令支持 `--json` 标志
- [ ] Commit 消息遵循 conventional 格式（`feat:`、`fix:`、`docs:`、`test:`）
- [ ] 已在本地测试过修改

## 测试结果

<!-- 贴上受影响测试的 `pytest -v` 输出。 -->

```
<在此粘贴测试输出>
```
